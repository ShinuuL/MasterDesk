[package]
name = "masterdesk-infrastructure"
version.workspace = true
edition.workspace = true
license.workspace = true

# Único crate autorizado a depender de sqlx (ADR-003). Implementações de
# tauri-plugin-* também moram aqui ou em src-tauri, nunca em domain/application.
[dependencies]
masterdesk-domain = { path = "../domain" }
async-trait = { workspace = true }

# sqlx entra de fato quando a migration real da Fase 2 for implementada.
# Deixado comentado propositalmente na Fase 1 para não travar o `cargo check`
# neste ambiente sandbox sem acesso a um banco/feature flags configurados.
# sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite"] }
